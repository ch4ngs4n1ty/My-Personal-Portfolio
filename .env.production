# Used automatically by `npm run build` / `npm run deploy` (production mode).
# Local dev uses .env.local instead, so the live site and localhost stay separate.
VITE_CHAT_API_URL=https://portfolio-chat.ch4ngs4n1ty.workers.dev
