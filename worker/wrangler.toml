name = "portfolio-chat"
main = "index.js"
compatibility_date = "2026-01-01"

# Origins allowed to call this Worker (comma-separated). Your live GitHub Pages
# site plus localhost for testing. Use "*" to allow any origin.
[vars]
ALLOWED_ORIGIN = "https://ch4ngs4n1ty.github.io,http://localhost:5173"

# The Claude model the chatbot uses. claude-opus-4-8 is the most capable.
# For a cheaper / faster portfolio Q&A bot, set this to "claude-haiku-4-5".
CLAUDE_MODEL = "claude-haiku-4-5"

# Set the API key as an encrypted secret (NOT here in plaintext):
#   npx wrangler secret put ANTHROPIC_API_KEY
